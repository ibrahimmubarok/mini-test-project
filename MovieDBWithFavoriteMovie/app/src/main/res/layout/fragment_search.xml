<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:recycler_view="http://schemas.android.com/apk/res-auto"
    tools:context=".fragment.SearchFragment"
    android:layout_margin="@dimen/margin_8dp">

    <ProgressBar
        android:id="@+id/progress_bar_search_movies"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        android:indeterminateDrawable="@drawable/progress"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <EditText
            android:id="@+id/edt_search_movies"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/ic_search_black_24dp"
            android:drawableStart="@drawable/ic_search_black_24dp"
            android:layout_margin="@dimen/margin_8dp"
            android:layout_toLeftOf="@id/btn_search"
            android:layout_toStartOf="@id/btn_search"
            android:textColor="@android:color/black"
            android:textSize="@dimen/item_18sp"
            android:inputType="text"/>

        <Button
            android:id="@+id/btn_search"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/search"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"/>

        <TextView
            android:id="@+id/tv_hasil_pencarian"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/margin_8dp"
            android:layout_marginStart="@dimen/margin_8dp"
            android:layout_below="@id/edt_search_movies"
            android:text="@string/hasil_pencarian"
            android:textStyle="bold"
            android:textSize="@dimen/item_18sp"
            android:visibility="gone"
            android:textColor="@android:color/black"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_search_movies"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_horizontal"
            android:layout_marginBottom="@dimen/margin_horizontal"
            android:layout_below="@id/tv_hasil_pencarian"
            tools:listitem="@layout/item_search_movies"
            tools:itemCount="1"
            recycler_view:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            android:overScrollMode="never">
        </androidx.recyclerview.widget.RecyclerView>
    </RelativeLayout>

</FrameLayout>